import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SchedulestopicViewPracticesResultPage } from './schedulestopic-view-practices-result.page';

describe('SchedulestopicViewPracticesResultPage', () => {
  let component: SchedulestopicViewPracticesResultPage;
  let fixture: ComponentFixture<SchedulestopicViewPracticesResultPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulestopicViewPracticesResultPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
