import { ComponentFixture, TestBed } from '@angular/core/testing';
import { HelpandSupoortPage } from './helpand-supoort.page';

describe('HelpandSupoortPage', () => {
  let component: HelpandSupoortPage;
  let fixture: ComponentFixture<HelpandSupoortPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(HelpandSupoortPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
