import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SchedulestopiceVisualizationExamPage } from './schedulestopice-visualization-exam.page';

describe('SchedulestopiceVisualizationExamPage', () => {
  let component: SchedulestopiceVisualizationExamPage;
  let fixture: ComponentFixture<SchedulestopiceVisualizationExamPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulestopiceVisualizationExamPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
