import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SchedulestopicexamPage } from './schedulestopicexam.page';

describe('SchedulestopicexamPage', () => {
  let component: SchedulestopicexamPage;
  let fixture: ComponentFixture<SchedulestopicexamPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulestopicexamPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
