import { ComponentFixture, TestBed } from '@angular/core/testing';
import { WorksheetPurchasedListTopicExamResultPage } from './worksheet-purchased-list-topic-exam-result.page';

describe('WorksheetPurchasedListTopicExamResultPage', () => {
  let component: WorksheetPurchasedListTopicExamResultPage;
  let fixture: ComponentFixture<WorksheetPurchasedListTopicExamResultPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(WorksheetPurchasedListTopicExamResultPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
