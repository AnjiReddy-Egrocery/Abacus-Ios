import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SchedulestopiceViewPracticesPage } from './schedulestopice-view-practices.page';

describe('SchedulestopiceViewPracticesPage', () => {
  let component: SchedulestopiceViewPracticesPage;
  let fixture: ComponentFixture<SchedulestopiceViewPracticesPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulestopiceViewPracticesPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
