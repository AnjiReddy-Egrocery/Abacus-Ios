import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SchedulestopicVisualizationResultPage } from './schedulestopic-visualization-result.page';

describe('SchedulestopicVisualizationResultPage', () => {
  let component: SchedulestopicVisualizationResultPage;
  let fixture: ComponentFixture<SchedulestopicVisualizationResultPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulestopicVisualizationResultPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
