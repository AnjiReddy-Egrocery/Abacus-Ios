import { ComponentFixture, TestBed } from '@angular/core/testing';
import { SchedulestopicexamResultPage } from './schedulestopicexam-result.page';

describe('SchedulestopicexamResultPage', () => {
  let component: SchedulestopicexamResultPage;
  let fixture: ComponentFixture<SchedulestopicexamResultPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(SchedulestopicexamResultPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
